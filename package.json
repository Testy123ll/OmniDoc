{
  "name": "omnidoc",
  "version": "0.1.0",
  "private": true,
  "description": "Universal File Converter, Editor & AI Workspace",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "start": "concurrently \"npm start -w frontend\" \"npm start -w backend\"",
    "install-all": "npm install && npm install -w frontend && npm install -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
